extends base.pug

block content
    div 
        p=video.description
        small=video.createdAt
    a(href=`${video.id}/edit`) Edit Video &rarr; <!--relative URL-->
    br
    a(href=`${video.id}/delete`) Delete Video &rarr; <!--relative URL-->
